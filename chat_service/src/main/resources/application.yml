server:
  port: 8082 # Chat service runs on 8082

spring:
  application:
    name: chat-service
  datasource:
    url: jdbc:mysql://localhost:3306/DentalClinicDB
    username: root
    password: 2003@Nethindu
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/